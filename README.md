# scrollxp
A miscellaneous of scroll effects using ScrollMagic.

To get it up and running, run:

```
$ npm install
```

Then:

```
$ cd scrollxp
$ npm link
$ cd ..
$ cd node_modules
$ npm link scrollxp
```

Then:

```
$ gulp
```
